# Developer tooling (lint, format, type, test). Layer on top of base.txt.
# Versions kept in step with the local venv so CI and laptops behave identically.
-c base.txt
ruff>=0.15,<0.16
black>=26.0,<27.0
mypy>=2.1,<3.0
pytest>=9.0,<10.0
pytest-asyncio>=1.4,<2.0
pytest-cov>=7.0,<8.0
pre-commit>=4.0,<5.0
pip-tools>=7.4,<8.0
types-redis>=4.6,<5.0
